
server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://mysqldb/dissertation_db
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: create
